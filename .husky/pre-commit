#!/usr/bin/env sh
# Staged files are fixed first so the final validation pass checks the code as
# it will actually be committed instead of failing on auto-fixable issues.
npx lint-staged
npm run lint
npm run format:check
